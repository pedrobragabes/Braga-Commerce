export default function AdminPanelLoading() {
  return (
    <section aria-live="polite" className="admin-empty" role="status">
      <span>…</span>
      <h2>Organizando a operação</h2>
      <p>Carregando os dados mais recentes da loja.</p>
    </section>
  );
}
